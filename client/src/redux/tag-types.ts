export enum tagTypes {
    user = "user",
    department = "department",
    faculty = "faculty",
    student = "student"
}

export const tagTypesList = [tagTypes.student,tagTypes.user, tagTypes.department, tagTypes.faculty];